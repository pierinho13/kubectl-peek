version: 2

project_name: kubectl-peek

before:
  hooks:
    - go mod tidy
    - go test ./...

builds:
  - id: cli
    main: .
    binary: kubectl-peek

    env:
      - CGO_ENABLED=0

    goos:
      - linux
      - darwin
      - windows

    goarch:
      - amd64
      - arm64

    ldflags:
      - -s
      - -w

archives:
  - id: cli
    ids:
      - cli

    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- title .Os }}_
      {{- .Arch }}

    formats:
      - tar.gz

    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: checksums.txt

source:
  enabled: true
  name_template: "{{ .ProjectName }}_{{ .Version }}_source_code"

changelog:
  use: github-native

release:
  github:
    owner: pierinho13
    name: kubectl-peek

homebrew_casks:
  - ids:
      - cli

    name: kubectl-peek

    repository:
      owner: pierinho13
      name: homebrew-tools
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"

    directory: Casks

    description: Simple interactive kubectl plugin for browsing Secrets and discovering where they are used
    homepage: https://github.com/pierinho13/kubectl-peek
    license: MIT

    binaries:
      - kubectl-peek

    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr",
              args: [
                "-dr",
                "com.apple.quarantine",
                "#{staged_path}/kubectl-peek"
              ]
          end

    commit_author:
      name: pierinho13
      email: TU_EMAIL